<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    {{{head}}}
    <title>Blog on Tupac</title>
  </head>

  <body>
    <div class="container">
          <h1>Tupac... dead or alive? The mystery never ends</h1>
          <!-- 'error' is key_name (key and value pairs) -->
          <h3> {{error}} </h3>
          <form method="post">
              <div class="form-group">
                  <input name='username' type="text" class="form-control" id="name" placeholder="Enter your username">
              </div>

              <button type="submit" class="btn btn-primary">Submit</button>
          </form>
      </div>
  </body>
</html>
